<%= form_with(model: verdict) do |form| %>
  <% if verdict.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(verdict.errors.count, "error") %> prohibited this verdict from being saved:</h2>

      <ul>
        <% verdict.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
